# Server Port
PORT=3000

# Environment: development | production
NODE_ENV=production

# Admin CMS Secret — ganti dengan random string minimal 32 karakter
# Generate contoh: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SECRET=ganti_dengan_random_secret_minimal_32_karakter_disini
